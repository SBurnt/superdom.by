{% from "./map.nunj" import map %}

<section class="contacts">
	<div class="container">
		<div class="section__inner">
			{{ map({
						info: true,
						map: true
					}) }}
		</div>
	</div>
</section>
